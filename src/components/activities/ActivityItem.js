import React from "react";
import { Text } from "react-native";

const ActivityItem = ({}) => {
  return (
    <React.Fragment>
      <Text>HI NAM</Text>
    </React.Fragment>
  );
};

export default ActivityItem;
